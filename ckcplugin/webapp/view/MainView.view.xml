<mvc:View 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="dongkuk.custom.plugin.ckcplugin.controller.MainView"
    busyIndicatorDelay="0"
    height="100%">
    <App>
        <Page>
            <!-- <headerContent>
                <Button
                    id="idBackButton"
                    visible="false"
                    text="{i18n>template.back.btn}"
                    tooltip="{i18n>template.back.tooltip}"
                    icon="sap-icon://nav-back"
                    press="onBackButtonPress" />
                <ToolbarSpacer />    
                <Title id="headerTitle" text="SAP DMC POD Plugin 개발 테스트" />
                <ToolbarSpacer />
                <Button
                    id="closeButton"
                    visible="false"
                    tooltip="{i18n>template.close.tooltip}"
                    icon="sap-icon://decline"
                    press="onClosePress" />
            </headerContent> -->

            <content>

                <!-- <FlexBox justifyContent="Center" alignItems="Center" width="100%" height="5%">
                    <Button id="alerttButton"
                            text="alert"
                            type="Accept"
                            press="onClickAlertButton" />
                </FlexBox> -->

                <Table items="{apiInventories>/content}" id ="tttable">
                    <headerToolbar>
                        <Toolbar>
                            <Title text="적재위치별 재고조회" />
                            <ComboBox items="{apiStorageLocations>/content}" width="100px" change=".onSelectCombo">
                                <items>
                                    <core:Item xmlns:core="sap.ui.core" key="{apiStorageLocations>storageLocation}" text="{apiStorageLocations>storageLocation}"/>
                                </items>
                            </ComboBox>
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column>
                            <Text text="inventoryId" />
                        </Column>
                        <Column>
                            <Text text="material" />
                        </Column>
                        <Column>
                            <Text text="storageLocation" />
                        </Column>
                        <Column>
                            <Text text="value" />
                        </Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{apiInventories>inventoryId}" />
                                <Text text="{apiInventories>material}" />
                                <Text text="{apiInventories>storageLocation}" />
                                <Text text="{apiInventories>quantityOnHand/value}" />
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
              
            </content>

        </Page>

    </App>
</mvc:View>